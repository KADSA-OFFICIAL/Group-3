# run-server.bat 은 CP949(ANSI 한국어) + CRLF 로 저장한다 (이슈 #201).
# cmd 의 배치 파서는 파일 안 위치를 바이트로 세기 때문에, UTF-8 로 저장하거나
# LF 로 줄을 끝내면 줄 위치가 어긋나 **줄 중간부터를 명령으로 실행한다**.
# 아래 줄이 어느 기기에서 받아도 CRLF 로 풀리게 보장한다. 인코딩은 git 이
# 지켜 주지 않으므로 편집할 때 사람이 지켜야 한다 (파일 머리말에 적어 두었다).
*.bat text eol=crlf
