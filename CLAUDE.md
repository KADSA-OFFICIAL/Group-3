# Group-3 Claude Harness

이 저장소는 issue-first workflow를 사용합니다. Claude Code는 새 기능, 버그 수정, 개선, 리팩터링, 동작 변경을 시작하기 전에 이 하네스를 따라야 합니다.

## 게임 정보 (젤리 워즈)

한 기기 한 화면 **1 VS 1 로컬 대전** 액션, **Godot 4.6 (GL Compatibility)**. 3조(스틱매너) 개발기획서 기반. `project.godot`의 `config/name`은 "Jelly Wars", 메인 씬은 `scenes/title.tscn`.
기획 핵심 루프(캐릭터·맵 선택 → **라운드마다 무기 선택** → 전투 → 3점 선취 승리)가 **전부 돈다.**

**2026-09-10에 온라인 구조를 걷어내고 오프라인 한 화면 2인으로 되돌렸다**(이슈 #320). 사용자 요청이다 — 서버컴이 꺼져 있거나 주소가 바뀌면 아무도 못 했고, 혼자 확인하려 해도 프로세스를 셋 띄워야 했다. 없어진 것: `Network`·`Lobby` 오토로드, 접속 화면·방·포트·Tailscale 주소, 관전 역할과 관전 빌드, 버전 악수, `@rpc`·`MultiplayerSpawner`·`MultiplayerSynchronizer`, `run-server.bat`·`docs/server.md`. **되살리지 말 것** — 필요해지면 `feat-320-offline-local-2p` 이전 이력에서 꺼낸다(2026-07-28~2026-09-09 사이의 커밋들).

**한 프로세스가 젤리 둘을 다 굴린다.** 판정 코드는 그대로 남았고 복제만 사라졌다 — `main.gd`가 여전히 공격·점수·라운드의 주인이고, `Player`의 공개 함수(`apply_hit`·`set_frozen`·`reset_round` …)가 그 결과를 받는 창구다. 예전의 `server_*` 한 겹과 `@rpc` 한 겹이 한 겹으로 합쳐졌다.

**젤리마다 키가 따로 있다** — 1P는 `WASD`+왼쪽 `Shift`, 2P는 방향키+`Space`다. 액션 이름은 `p1_left`·`p2_left`처럼 자리가 앞에 붙고, `Player`는 `GameState.action(player_id, ...)`로 만든 이름만 읽는다. 이 배치는 온라인 전환(#33) 이전에 쓰던 것과 같다.

**플레이어 번호는 1과 2다.** 자리(slot)는 0·1이고 `번호 = 자리 + 1`이며, 변환은 `GameState.id_at()`·`slot_of()`만 쓴다. 전투 화면의 표(점수·쿨타임·출혈)가 전부 이 번호를 열쇠로 쓰고, 노드 이름도 `Player_1`·`Player_2`다.

`main` 브랜치는 2026-07-26에 새 구현(커밋 `e2a7dcb`)으로 교체되었다. 이전 구현(`autoload/game_manager.gd`, `scripts/weapons/`, `scenes/maps/` 등)은 `backup/main-before-reset` 브랜치에만 있고 현재 코드베이스에는 없다 — 그 경로를 참조하지 말 것.


title(`StartButton`으로 시작 — 고를 것이 없다) → select(1P·2P가 **각자 캐릭터를 고르고** `시작!`) → main(전투, **라운드가 시작될 때마다 무기 카드 3장이 1P → 2P 차례로 뜬다**, 씬이 열리면 젤리 둘을 바로 세운다) → **3점 선취 시 select로 돌아간다**(`_return_to_select()`). 전투 중 ESC(`ui_cancel`)로 title 복귀.

**경기가 끝나면 씬이 통째로 새로 열린다** — 점수·표를 손으로 비우던 `_server_reset_match()`가 없어진 이유다. 전용 서버가 씬에 머물던 시절에만 필요했다.

### 구조 요약

- `scripts/game_state.gd` = 오토로드 싱글턴 `GameState`: **화면과 화면을 잇는 유일한 상태**(이슈 #320). `CHARACTERS`(`Characters.names()` 5종 — 사본을 두지 않고 캐릭터 표에서 만든다), `PLAYER_COUNT`(2)·`PLAYER_IDS`(`[1, 2]`), 자리별 `configs`(character)와 `config_for()`·`set_config()`, 번호↔자리 변환 `id_at()`·`slot_of()`, 입력 이름 `action(player_id, name)`. **무기 목록도 맵 목록도 여기 없다** — 무기는 이슈 #205, 맵은 그 뒤 요청으로 빠졌다. 둘 다 선택 창이 아니라 라운드가 시작될 때 `main.gd`가 뽑는다(`Weapons.random_choices()`·`_pick_round_map()`).
- `scenes/title.tscn` + `scripts/title.gd` = 타이틀 화면. `StartButton`("시작")과 `StatusLabel`(조작 안내)뿐이고 누르면 select로 전환한다.
  - **화면 전체가 표지 원화다**(요청) — `Cover`(TextureRect)가 `assets/ui/cover.png`를 `KEEP_ASPECT_COVERED`로 깔고, **바닥에 맞춰** 1152×768 칸을 두어 위로 넘치는 120px만 화면 밖으로 보낸다(`match_intro.tscn`과 같은 방식). 로고와 아래쪽 띠가 온전히 남고 눌리지도 않는다.
  - **원화에 로고와 젤리가 이미 들어 있으므로 `TitleLabel`(`젤리 워즈`)·`JellyLeft`·`JellyRight`를 없앴다.** 셋 다 `title.gd`가 읽지 않는 순수 장식이었다. 되살리면 그림의 로고·젤리와 두 번 겹친다.
  - **남은 조작은 원화를 피해 네 귀퉁이로 갔다.** 원화의 로고가 화면 가운데(`y 300~525`)를 차지해서 그 아래에는 120px밖에 없다 — 가운데에 세로로 쌓으면 글자가 로고 위에 겹친다. `AddressLabel`·`AddressValue`는 왼쪽 위, `RoomLabel`·`RoomBox`는 오른쪽 위, `SubLabel`은 왼쪽 아래, `StatusLabel`은 아래 가운데다.
  - **`StartButton`은 원화에 그려진 띠 자리에 딱 맞춰 놓았다**(`x ±278`, `y 550~606`). 그 띠는 그림상 눌릴 것처럼 생긴 홈이라, 거기에 버튼을 앉히면 장식이 곧 조작이 된다.
  - 원화 위에 얹는 글자는 **흰 글자 + 진한 테두리**다(전투 화면의 `Banner`와 같은 방식). `OBSERVER_COLOR`도 밝은 라벤더로 올렸다 — 진한 라벤더는 어두운 꽃밭 위에서 테두리와 뭉쳐 읽히지 않았다.
  - `assets/ui/cover.png`는 **첫 화면과 경기 표지(`match_intro.tscn`)가 같이 쓴다.** 그래서 이름이 `match_intro.png`가 아니다 — 한쪽에서만 쓰는 그림처럼 보이면 다른 쪽을 고칠 때 눈치채지 못한다.
  - **고를 것이 하나도 없다**(이슈 #320) — 주소 라벨(`AddressLabel`·`AddressValue`)과 방 버튼(`RoomLabel`·`RoomBox`)을 통째로 걷어냈고 `StartButton`은 `시작`이다. `StatusLabel`에는 접속 상태 대신 **조작 안내**(`CONTROLS_TEXT`)를 적는다 — 게임 안에서 키를 알려 주는 곳이 여기와 select 두 곳뿐이다.
- `scenes/select.tscn` + `scripts/select.gd` = 캐릭터 선택 창. `P1Panel`/`P2Panel`은 흰 카드(`Card`) 위에 얹히고 **둘 다 조작할 수 있다**(이슈 #320) — 한 화면이라 "내 패널"이 따로 없다. 고른 값은 `config_changed` 신호를 타고 `GameState.set_config()`로 들어가고, `GoButton`(`시작!`)이 그 둘을 한 번 더 적은 뒤 main으로 넘어간다(신호를 놓친 칸이 있어도 화면에 보이는 것이 그대로 전투로 간다). `StatusLabel`에는 조작 안내를 적는다. **가운데 칸이 아예 없다** — 화면은 `1P 패널 | StatusLabel + GoButton | 2P 패널` 세 칸이다.
  - **화면 꾸밈은 `Decor`(`scripts/select_decor.gd`)가 통째로 그린다**(요청, 사용자가 준 목업 기준). `BG` 다음·나머지 전부보다 앞에 두어 카드·버튼·글자가 이 위에 얹힌다. **원화 파일이 아니라 `_draw()`다** — 목업은 그림 한 장이었지만 이 저장소의 연출이 전부 `_draw()`이므로 같은 방식으로 옮겼고, 그래서 화면 크기를 따라오고 색은 상수 한 줄로 바뀐다. 들어 있는 것: 왼쪽 분홍·오른쪽 남색 **네 귀퉁이 그라데이션**(세로 띠 8px로 잘라 그린다), 구름 빛무리, 가운데를 지르는 **번개**, 별·십자 반짝임, 떠 있는 **젤리 조각**, 아래쪽 꽃밭 띠, 가운데 **`VS`**(V는 분홍·S는 파랑으로 따로 그린다), 맨 위 **`캐릭터 선택` 리본**. 자리는 `SEED`로 고정해 켤 때마다 같은 하늘이 나온다.
    - **꽃밭 띠는 세로 조각으로 그린다** — 윗변이 물결쳐 오목한 다각형이 되는데 `draw_colored_polygon()`은 볼록한 것을 전제로 삼각형을 나눠서 오목한 자리가 엉뚱하게 메워진다.
    - **번개 빛무리는 꺾이는 지점에만 놓지 않는다** — 큰 빛무리 여덟 개를 놓으면 이음선에 동그란 얼룩이 여덟 개 생긴다. 선을 따라 촘촘히 나눠 작게 놓아야 한 줄기 빛이 된다.
    - **젤리 조각은 카드에 가리지 않는 자리에만 놓는다** — 카드가 화면 좌우 3분의 2를 덮으므로 아무 데나 뿌리면 열두 개 중 두세 개만 보인다. 절반은 카드 위쪽 띠, 절반은 두 카드 사이 가운데 칸이다.
    - `StatusLabel`은 **흰 글자 + 진한 테두리**다. 배경이 진한 분홍·남색이 되면서 예전의 진한 자주색 글자는 묻혔다.
  - **카드는 편마다 색이 다르다**(요청) — `player_panel.gd`의 `accent`(1P 분홍 `(0.96, 0.55, 0.78)`·2P 라벤더 `(0.56, 0.59, 0.91)`)를 씬에서 정하고, `_apply_accent()`가 카드 바탕·테두리·랜덤 버튼 네 상태·캐릭터 버튼 글자·별 배지를 거기서 만들어 낸다. 색이 흩어지지 않게 **씬에서 정하는 값은 이 속성 하나뿐**이다(테마의 `Panel` 스타일은 한 벌뿐이라 편마다 다른 카드를 담을 수 없다 — `CLAUDE.md`의 "색은 테마에서" 규칙은 화면 전체에 걸리는 색을 말한다).
    - **스타일박스는 반드시 `duplicate()`해서 쓴다.** 씬에 박아 둔 `SubResource`는 그 씬을 여러 번 만들어도 **같은 객체 하나를 나눠 쓴다** — 복제하지 않고 색을 바꾸면 1P를 칠하는 순간 2P 카드까지 분홍이 된다.
    - **`disabled` 모습도 함께 칠한다** — 상대 패널은 늘 잠겨 있어서 이것을 빼면 두 카드 중 한쪽 버튼만 회색으로 남는다.
    - 별 배지는 `scripts/panel_star.gd`이고 **씬의 맨 마지막 자식**이다. 부모의 `_draw()`는 자식보다 먼저 그려지므로 `player_panel.gd` 안에서 그리면 `Card`에 덮인다. 2P는 `_ready()`에서 앵커를 오른쪽으로 옮겨 카드의 바깥 위 모서리에 단다.
  - **`MapBox`·`WeaponBox`를 없앴다**(요청). 맵도 무기도 라운드가 시작될 때 전투 화면에서 정해지므로(`main.gd`의 `_start_round`·`_begin_pick_phase`) 이 화면에서 고를 것이 없고, 남아 있던 것은 "라운드마다 정해집니다"라고만 적힌 안내판 둘이었다. 치운 자리(`x 436~716`의 `y 50~480`)에 `StatusLabel`(`y 300`)과 `GoButton`·`RoomSwitcher`(`y 352`)를 올렸다 — 안 올리면 화면 가운데에 280×430 짜리 빈 구멍이 남는다. **되살리지 말 것**: 고를 수 없는 칸이 화면 가운데를 차지하고 있었다.
- `scenes/weapon_pick.tscn` + `scripts/weapon_pick.gd` = **라운드 시작 무기 선택 카드**(이슈 #205). `UI/HUD` 아래 전체 화면 오버레이이고, 카드 3장(`Cards/Card0~2`)에 이름·그림(`weapon_preview.gd`)·설명(`Weapons.description()` — "무기 증강 설명 리스트" 문서의 문구)이 들어간다. **보여주고 누르는 일만 한다** — 후보를 뽑는 것도 고른 것을 확정하는 것도 `main.gd`다. 카드를 누르면 `weapon_chosen(index)`(**자리 번호**)를 내보낸다. **한 화면이라 1P → 2P 차례로 연다**(이슈 #320): `open(choices, seconds, player_id)`가 받은 번호가 지금 고르는 사람이고, 마우스로 눌러도 되고 **그 사람 키로 골라도 된다**(좌우로 짚고 `skill`·`jump`로 정한다 — 키보드 앞에 둘이 앉아 있으므로 마우스 하나를 주고받게 하지 않는다).
  - **고르는 동안 두 젤리는 얼어 있다**(`Player.frozen` → `can_act()`가 false) — 카드를 읽는 사람이 그 자리에서 맞으면 안 된다. 제한 시간은 **차례마다** 20초(`main.gd`의 `WEAPON_PICK_TIME`)이고, 다 되면 `_auto_pick()`이 그 차례 몫을 대신 뽑는다. 앞사람이 오래 골랐다고 뒷사람 몫이 줄면 고를 새도 없이 대신 뽑히게 된다.
  - **라운드는 `_start_round()` → `_begin_pick_phase()` → `_open_pick_turn(1P)` → `_take_pick()` → `_open_pick_turn(2P)` → `_finish_pick_phase()` 순서로 열린다.** 후보는 **사람마다 따로** 뽑고 한 사람 안에서는 겹치지 않는다. 강화 폭탄·표창 뽑기(`_roll_empowered`)는 무기가 정해진 뒤인 `_finish_pick_phase()`에서 한다 — 지난 라운드 무기로 뽑으면 뜻이 없다.
  - **첫 라운드도 여기서 연다** — 씬이 열리며 젤리 둘을 세운 `_spawn_players()`가 곧바로 `_start_round()`를 부른다.
- `scenes/player_panel.tscn` + `scripts/player_panel.gd` = 플레이어 1인 패널(양쪽 재사용). `mirrored`가 true면 아이콘 열을 오른쪽으로 옮긴다. 캐릭터 버튼이 목록을 순환하고, `RandomButton`은 전부 랜덤. **무기 버튼은 없다**(이슈 #205) — 무기는 라운드마다 전투 화면에서 고른다. **젤리 위에도 아무것도 없다**(요청) — `Player` 한 줄만 든 `PlayerDropdown`(OptionButton)이 있었는데 고를 것이 하나뿐이라 누를 수 없었고 읽는 곳도 없었다. 치우면서 `Preview`가 그 높이를 받아 젤리가 커졌다. 다시 넣지 말 것: 1P·2P 구분은 패널 위치와 카드 색이 이미 한다. 사용자 조작으로 값이 바뀌면 `config_changed`를 내보내고, `apply_config()`는 들고 있던 값을 표시만 한다(이때는 시그널을 내보내지 않는다). **잠그는 함수는 없다**(이슈 #320) — 두 패널 다 조작할 수 있게 되면서 `set_interactive()`·`set_display_only()`를 지웠다.
- `scripts/weapon_preview.gd` = 무기 그림 미리보기. **라운드 시작 선택 카드**(`weapon_pick.tscn`)가 쓴다(이슈 #205). `jelly_preview.gd`와 같은 형태이고 `Art.content_rect()`로 여백을 뺀다. 그림이 있는 무기가 15종뿐이라 **없으면 아무것도 그리지 않고** 옆의 이름 라벨이 대신한다. **손에 든 그림이 아니라 `Weapons.preview_texture()`를 쓴다**(이슈 #173) — 너클처럼 착용 모습(뻗은 주먹)과 무기 자체(금속 너클)가 다른 무기는 선택창에 무기 쪽을 보여야 한다. 무기 원화는 세로로 긴 것(검 1:4.7)과 가로로 긴 것(전기톱·대포 총·샷건)과 뭉툭한 것(글러브 1.18:1)이 섞여 있어 칸은 세로로 잡았다.
- `scripts/jelly_preview.gd` = 젤리곰 미리보기. `character_id`와 `pose` setter가 둘 다 `_refresh()`를 지나 `Characters.pose_texture()`로 그림을 받고 `queue_redraw()`를 호출하며, `_draw()`가 비율을 지켜 가운데에 그린다. **한쪽만 갱신하면 나중에 지정한 값이 앞의 것을 지우므로 반드시 같은 곳을 지나가게 한다.** `pose`를 비워 두면 평소 그림이라 선택 창 패널·타이틀 젤리는 손댈 것이 없다(이슈 #178).
- `scripts/map_preview.gd` = 선택 창 맵 카드의 배경 그림(이슈 #289). `Maps.art_texture()`의 배경 원화를 **칸을 꽉 채우도록** 비율을 지켜 잘라 그린다(aspect-fill) — 캐릭터·무기 미리보기가 여백을 두고 다 넣는 것(aspect-fit)과 반대다. 배경이라 여백이 남으면 카드가 뚫려 보인다. 원화가 없는 맵(바다)·`랜덤`·빈 자리는 `Maps.art_color()`의 단색으로 채운다.
  - **둥근 모서리는 이 노드가 만들지 않는다** — 뒤에 깔린 `ScreenRect`(둥근 판)보다 8px 안쪽에 놓아 그 판이 테두리로 보이게 한다. `clip_children`으로 잘라 보려 했지만 **GL Compatibility 렌더러에서는 동작하지 않아**(CanvasGroup 미지원) 모서리가 각지게 남았다. 이 프로젝트에서 `clip_children`은 쓸 수 없다.
  - 원화 위에 덮는 `scrim`(0.12)은 **카드 톤을 맞추는 장식이고 글자 가독성을 떠맡지 않는다**(#112). 흰 글자는 불투명한 배지(`QBadge`) 위에만 둔다.
- `scripts/characters.gd`(`class_name Characters`) = **캐릭터 표 5종**(분홍·파랑·초록·노랑·빨강). 이름과 그림 경로의 유일한 출처이며 선택 창 선택지·선택값 정리·전투 화면 그림이 모두 여기서 나온다. 그림은 `assets/characters/`에 있고, 파일이 없으면 표의 몸통 색 단색으로 대신 그린다. 여백 측정은 `Art.content_rect()`가 한다.
  - **캐릭터마다 포즈가 셋이다**(이슈 #176) — 평소(`file`)·승리(`win_file`)·패배(`lose_file`). `pose_texture(name, pose)`가 골라 주고 그 포즈의 파일이 없으면 조용히 평소 그림으로 되돌아간다(무기의 `preview_file`과 같은 방식). 포즈 이름 상수는 `POSE_IDLE`·`POSE_WIN`·`POSE_LOSE`이고 **네트워크로 오가는 값**이다. 쓰는 곳은 전투 화면의 젤리와 결과 화면 미리보기다 — 선택 창 패널·타이틀 젤리는 포즈를 지정하지 않아 평소 그림 그대로다.
- `scenes/main.tscn` + `scripts/main.gd` = 전투 화면이자 **공격 판정의 주인**. 지형은 씬에 없고 라운드가 열릴 때마다 `_pick_round_map()`이 뽑아 `_load_map()`이 `MapRoot` 아래에 붙인다(젤리를 세우기 전에). 화면 글자는 전부 `UI/HUD` 아래 흰 카드 안에 있다 — 맨 위 가운데 `ScoreCard`(`P1Score`·`Divider`·`P2Score`, 라운드 포인트를 좌우 대칭으로, 1P 핑크·2P 라벤더)와 **화면 좌우 맨 위에는 아무것도 없다**(이슈 #317) — 체력은 맞은 순간 젤리 머리 위에 2초 뜨는 `scripts/health_bar.gd`가 내고, 이름·무기 카드(`P1Card`·`P2Card`)는 없앴다(이름은 젤리 머리 위 `NameLabel`, 무기는 손에 든 그림이 낸다). ESC로 title 복귀.
  - **맵은 판마다 갈린다**(#310) — `_ready()`가 까는 것은 첫 프레임의 빈 화면을 막는 임시 지형이고, 진짜 지형은 `_start_round()`가 `_pick_round_map()`(섞은 가방에서 한 장씩)으로 뽑아 깐다. 그래서 `_load_map()`은 **두 번째 호출이 깨끗해야 한다**: 옛 지형은 `queue_free()` 전에 `remove_child()`로 **먼저 떼어 내고**(프레임 끝까지 물리 공간에 남아 새 맵과 겹친다), `_hazard`도 null로 되돌린다(즉사 구역이 없는 맵으로 갈면 해제된 옛 노드를 들고 있는다).
  - **HUD 카드는 화면 맨 위 `y 8~110`에 붙어 있다**(이슈 #267). 예전에는 `HintCard`(조작 안내)·`MapCard`(맵 이름)가 `y 8~78`을 차지하고 플레이어 카드가 그 아래 `y 88~190`에 있었는데, 배경 원화 맵(#264)의 높은 발판에 선 젤리가 카드에 통째로 가려졌다. 조작 안내와 맵 이름표를 **없애고** 플레이어 카드를 그 자리로 올렸다 — 조작은 몇 판이면 외우고 맵 이름은 선택 창에서 이미 골랐다. 새 글자를 화면 위에 얹을 때는 **이 띠를 다시 두껍게 만들지 말 것**: 두꺼워진 만큼 설 수 없는 발판이 늘어난다.
  - **UI는 `UI`(CanvasLayer) 아래에 둔다**(이슈 #82). 씬 루트가 `Node2D`라 Control을 거기에 바로 붙이면 앵커가 기준으로 삼을 부모 사각형이 0×0이 되어 `HUD`의 크기도 0이 된다 — 그러면 `anchor_left = 0.5`로 가운데를 잡은 것들이 전부 화면 왼쪽 끝(x=0)에 그려지고 화면을 덮는 판도 안 보인다. CanvasLayer 아래에서는 뷰포트 크기가 기준이 된다. 앵커를 쓰는 UI를 새로 넣을 때는 반드시 이 아래에 붙인다.
  - **글자는 맵 배경 위에 그냥 얹지 않고 흰 카드 안에 넣는다**(이슈 #112). 맵마다 배경 밝기가 정반대라서(평지 하늘 `(0.82, 0.93, 0.99)` ↔ 용암 `(0.42, 0.26, 0.38)`) 한 맵에 맞춘 글자색은 다른 맵에서 사라진다 — 진한 글자를 용암 배경에 얹으면 대비가 1.3:1이다. 카드를 깔면 배경이 무엇이든 10:1이 나오므로, 맵 위에 글자를 새로 얹을 일이 생기면 카드부터 만든다. 카드는 하늘 영역(y ≲ 110)에 둔다 — 점프 정점이 y≈340이라 지형·젤리와 겹치지 않는다.
  - 체력 숫자는 막대 **안**(`show_percentage`)이 아니라 카드 위 별도 라벨에 적는다. 막대 안에 그리면 채운 쪽과 빈 쪽의 밝기가 반대라 어느 색을 골라도 한쪽에서 묻힌다.
  - **플레이어는 씬에 배치되어 있지 않고 `_spawn_players()`가 씬이 열릴 때 세운다**(이슈 #320) — `PLAYER_SCENE.instantiate()` 로 만들어 `Players` 아래에 붙인다. 노드 이름은 `Player_1`·`Player_2`이고, 자리·캐릭터는 `GameState`가 들고 있던 것을 그대로 쓴다. 기다릴 접속이 없으므로 스포너도 준비 신호도 없다.
  - 투사체도 같은 방식이다 — `_spawn_projectile(data)`가 만들어 `Projectiles` 아래에 붙이고, 다 쓰면 `queue_free()`한다.
  - **연출(이펙트)은 `_play_*()` 함수로 띄운다**(이슈 #99). 판정이 결과를 정한 뒤 `Effects`(z_index 50) 아래에 노드를 붙이고, 그 노드가 다 재생한 뒤 스스로 `queue_free()`한다 — 아무것도 맞히지 않고 잠깐 떴다 사라져서 위치를 계속 맞출 것도 나중에 지워 줄 것도 없다. 검 특수의 빛기둥(`scenes/light_burst.tscn`)이 첫 사례다.
  - `_physics_process()`가 전투 틱 전체를 돈다: `_check_basic_attacks()`(근접 접촉·원거리 자동 발사) → `_check_pending_specials()`(강제 이동 중 명중) → `_tick_bleeds()`(출혈) → `_tick_bursts()`(연발 — 소총은 **시간**으로, 글러브 로켓 펀치는 **발 수**로 끝난다, 이슈 #164. `_start_burst()`가 둘 다 예약하고 `first_knockback`으로 첫 발만 세게 민다) → `_check_falls()`(낙사) → `_tick_round()`(예약된 라운드 재시작·선택 창 복귀). 특수 공격은 `Player.special_requested` 신호를 받아 `_execute_special()`에서 무기별로 분기한다. **틱이 아니라 신호로 오는 판정이 하나 더 있다** — 양날 도끼가 땅에 닿으면 `Player.landed_forced`가 뜨고 `_on_forced_landed()`가 그 자리 반경 안을 때린다(이슈 #167).
  - **근접은 기본·특수 모두 `_faces()`를 통과해야 들어간다**(이슈 #107) — 등 뒤의 상대는 못 때리고, 뒤를 잡으면 일방적으로 때릴 수 있다. 좌우가 정확히 같은 순간(위아래로 겹침)은 빗나간 것으로 본다. **원거리**(`_server_fire()`가 이미 `facing` 쪽으로만 쏜다)와 **강제 이동 중의 특수**(`_check_pending_specials` — 도끼 낙하는 바로 아래를 때려서 좌우를 따지면 영영 안 맞는다)는 일부러 거치지 않는다.
  - **포인트 진행도 여기가 주인이다.** `_on_player_died()`가 상대에게 1포인트를 주고, `Combat.POINTS_TO_WIN`(3포인트)에 닿으면 승리를 표시한 뒤 `_return_to_select()`로 선택 창으로 돌려보낸다. 아니면 `ROUND_RESTART_DELAY`(2초) 뒤 `_start_round()`가 투사체·판정 타이머를 비우고 `Player.server_reset()`으로 양쪽을 되살린다. 점수(`scores`)와 안내 문구(`banner`)는 `main.gd` 가 정하고 `_update_round()` 가 HUD에 적는다.
  - **화면 문구는 "라운드 승패"가 아니라 "포인트 획득"으로 쓴다**(이슈 #76) — 배너는 `1P +1 포인트`, 마지막에 `1P 승리!  3포인트 달성`, HUD 점수는 `_score_text()`가 `●○○  1 / 3`처럼 동그라미와 숫자를 같이 낸다. 규칙은 그대로이고 표현만 통일한 것이다.
  - **판은 `3 · 2 · 1 · START!` 를 세고 열린다**(요청) — `scenes/countdown.tscn` + `scripts/countdown.gd`. 무기를 고른 직후(`_finish_pick_phase`)에 뜨고, 그 1.68초 동안 두 젤리는 **자기 자리에 선 채로 얼어 있다**. 한 칸이 `Combat.COUNTDOWN_STEP`(0.42초)이고 넷이라 `COUNTDOWN_TIME`은 1.68초다 — **판마다 끼므로 빠른 쪽으로 잡았다**(3점 경기면 다섯 번까지 본다). `countdown.gd`가 두 상수를 그대로 읽으므로 값은 한 곳에만 있다.
    - **얼음을 푸는 것은 카운트다운 화면이 아니다.** `_finish_pick_phase()`가 `_round_opens_at`을 예약하고 `_tick_round()`가 그때 `_open_round()`로 푼다 — 화면이 조금 먼저 `START!`를 지웠다고 먼저 움직여지지 않는다.
    - **`_open_round()`이 `server_reset()`을 한 번 더 부른다.** 그 함수가 `ROUND_START_GRACE`(2초 무적)를 부르는 순간부터 재기 때문이다 — 무기 선택이 끝날 때 준 무적은 세는 데 쓴 1.68초가 그 안에서 흘러가 실제 싸움이 시작될 때 0.3초만 남는다. 젤리가 얼어 있어 자리가 그대로이므로 다시 불러도 순간이동으로 보이지 않는다.
    - **세는 도중에 판이 접히면(`_abandon_match`) 이 예약을 버린다** — 안 버리면 접힌 경기에서 젤리를 다시 자리로 되돌려 놓는다.
    - **뒤에 막을 깔지 않는다** — 세는 동안 두 사람은 서로의 자리와 손에 든 무기를 보고 첫 움직임을 정한다. 대신 글자를 크게(숫자 180·`START!` 128) 하고 테두리를 굵게(28) 둘러 어떤 맵 위에서도 읽히게 한다. `Banner`가 쓰는 것과 같은 방식이다(`_start_round`가 배너를 비우므로 글자끼리 겹치지 않는다).
  - **포인트가 오를 때는 전용 장면이 뜬다**(이슈 #273) — `scenes/point_gain.tscn` + `scripts/point_gain.gd`. 띠가 왼쪽에서 쓸려 들어와 딴 사람의 젤리 얼굴(승리 포즈)·이름·포인트 칸 3개를 보여주고, **이번에 딴 칸**에 흰 빛이 터지며 금색으로 채워진 뒤 `+1 포인트!`가 뜬다. 그림 파일 없이 `_draw()`로만 그리고 얼굴만 캐릭터 표에서 가져온다. 실제 블러는 쓰지 않는다 — GL Compatibility에서 화면 텍스처 블러는 셰이더가 필요하므로 옅은 흰 막으로 뒤를 물러나게 한다.
    - **띠 색은 늘 딴 사람 기준(파랑)이다**(이슈 #320) — 두 기기 시절에는 같은 신호를 받고도 딴 쪽에는 파랑이, 잃은 쪽에는 빨강이 떴지만 한 화면에서는 가를 곳이 없다. `point_gain.play()`의 `as_gain` 인자는 남아 있고 `main.gd`가 늘 `true`를 준다. 채워진 칸은 양쪽 다 금색이다(포인트라는 물건의 색이므로). 마지막 3점째는 `is_final`로 빛과 고리가 훨씬 크게 터진다.
    - **장면이 도는 자리는 판 사이의 빈 시간이다.** `Combat.POINT_GAIN_TIME`(2.6초)이 장면 길이이고 `point_gain.gd`의 `TOTAL`이 그것을 그대로 읽는다 — 상수를 양쪽에 따로 두면 한쪽만 고쳤을 때 조용히 어긋난다(`match_intro.gd`가 그 꼴이다). `ROUND_RESTART_DELAY`는 **그보다 길어야 한다**(2.0 → 2.8초): 짧으면 장면이 끝나기 전에 다음 판의 무기 선택 카드가 그 위에 뜬다.
    - **마지막 포인트에서는 결과 화면을 뒤로 미룬다** — 점수가 난 자리에서 `_show_match_result()`를 바로 부르면 축하 장면 위에 승패 글자가 덮인다. `_result_at`·`_result_winner`에 예약해 두고 `_tick_round`가 때가 되면 부르며, 선택 창 복귀도 `POINT_GAIN_TIME + MATCH_END_DELAY`로 밀린다(`MATCH_END_DELAY`는 결과 화면이 실제로 떠 있는 시간이라야 한다). 상대가 나가 판이 접히면 `_abandon_match()`가 이 예약을 버린다 — 안 버리면 접힌 경기 위에 승리 화면이 뒤늦게 뜬다.
    - `UI/HUD` 안에서 `Banner` 다음, `ResultOverlay` **앞**에 둔다 — HUD 카드와 배너는 덮어야 하고(장면이 화면을 가져가는 것으로) 결과 화면·무기 선택 카드에는 덮여야 한다.
  - **경기가 끝나면 이긴 쪽 기준으로 결과 화면을 한 번 띄운다**(이슈 #79·#320) — `_show_match_result(winner_id)`가 `HUD/ResultOverlay`(어둡게 덮는 `Dim` + `Jelly` 미리보기 + `ResultLabel` + `ScoreLabel`)에 트윈으로 승리 연출(통통 튐 + 글자 팝업·맥동)을 만들고 글자만 `1P 승리!`로 바꾼다. 젤리는 `jelly_preview.gd`를 재사용하고 **이긴 사람의 캐릭터**를 승리 포즈로 보여준다(이슈 #178) — 방금 전투 화면에서 보고 있던 포즈가 그대로 이어진다. 두 기기 시절에는 진 쪽이 패배 연출을 봤지만 한 화면에서는 갈라 놓을 곳이 없어 `_play_lose()`를 지웠다. 결과 화면이 떠 있는 동안 `Banner`는 접힌다. 연출 트윈은 `_result_tweens`에 모아 두고 `_hide_result()`가 전부 끊는다.
  - **판이 접히는 길은 없다**(이슈 #320) — 사람이 빠질 수 없으므로 `_abandon_match()`·`_tick_abandon()`·`_fighter_count()`를 지웠다. 경기가 끝나면 `_return_at`이 되어 `_return_to_select()`가 씬을 갈아탄다.
- `scenes/player.tscn` + `scripts/player.gd`(CharacterBody2D, `class_name Player`): 젤리 하나. `player_id`(1·2)·`player_name`·`character_id`·`weapon_id` export. SPEED 320, JUMP_VELOCITY -560, FAST_FALL_MULTIPLIER 2.0.
  - **자기 자리의 키만 읽는다**(이슈 #320) — `read_input()`이 `GameState.action(player_id, ...)`로 만든 이름(`p1_left`·`p2_skill` …)만 보므로 한 키보드에서 둘이 서로 간섭하지 않는다. `_read_input()`이 매 프레임 그것을 읽어 두고 `_take_input()`이 꺼내 쓴다 — 점프는 눌림을 한 번만 소비하고, 특수는 눌림·뗌이 **바뀌는 순간**에만 시간을 재기 시작하거나 발동한다(그 엣지를 흘리면 `LONG_PRESS_TIME` 구분이 서지 않는다).
  - `_physics_process()`가 `_read_input()` → `_check_long_press()` → `apply_movement()` → 그림 갱신 순서로 돈다. `move_and_slide()`는 `apply_movement()` 안에서만 부른다. 보간도 상태 수신도 없다 — 계산하는 곳과 그리는 곳이 같다.
  - 전투 상태(`hp`·`alive`·`facing`·무적·기절·게이지·버프·강제 이동)는 **`main.gd`의 판정이 정하고** 공개 함수(`apply_hit`·`apply_dot`·`kill`·`reset_round`·`teleport`·`apply_buff`·`set_weapon`·`set_frozen`·`set_pose`·`set_gauge`·`set_special_ready`·`set_empowered`·`start_swing`·`start_forced`·`end_forced`)가 그 결과를 적는다. 판정 자체는 여기가 아니라 `main.gd`에 있다.
  - 방패의 짧게/길게는 `_check_long_press()`가 잰다 — 길게가 확정되는 순간 뗄 때를 기다리지 않고 바로 발동한다.
  - **강제 이동 중에는 조작이 잠긴다**(`_apply_forced()`) — 전기톱 돌진(`"dash"`), 양날 도끼 상승·정점·낙하(`"rise"`·`"hover"`·`"fall"`). 다만 무기 표에 `special_air_control`이 있으면 **뜬 동안 좌우 입력을 받는다**(양날 도끼, 이슈 #167). 1초 가까이 조작이 잠기는 동안 상대가 걸어서 낙하 지점을 벗어나 쿨타임 9초짜리가 거의 빗나가고 있었다. **돌진에는 주지 않는다** — 바라보는 쪽으로 내지르는 기술이라 꺾이면 다른 기술이 된다. 조종은 `_steer()` 한 곳에 있고, 입력이 없으면 가로 속도를 0으로 둔다(뜨기 직전 달리던 속도가 남으면 조종하지 않았는데도 옆으로 흐른다). 낙하가 땅에 닿으면 `landed_forced`를 내보내고 **누구를 때리는지는 `main.gd`가 정한다** — 전투 판정의 주인이 거기다.
  - 몸은 `Body`(Sprite2D)에 캐릭터 그림을 붙인다. 원화가 정사각 캔버스에 여백을 두고 그려져 있어 `Characters.content_rect()`로 **투명 여백을 뺀 실제 그림 영역**을 재고, 그 높이를 `BODY_HEIGHT`(72px)에 맞춰 배율과 위치를 정해 발을 충돌 상자 바닥에 붙인다. 찌그러짐은 그 기본 배율에 곱하고, 좌우 반전은 `facing`으로 `flip_h`를 켠다. **배율이나 반전이 바뀌면 반드시 `_place_body()`를 부른다**(이슈 #85) — Sprite2D는 자기 위치를 *중심으로* 확대·축소하므로 세로로 늘어나면 발이 바닥을 뚫고 납작해지면 발이 뜬다. 그래서 여백 보정은 배율을 곱하기 전 값(`_body_offset_unit`)으로 들고 있다가 `_place_body()`가 그때의 배율로 환산해 **발밑을 항상 `BODY_BOTTOM`에 고정**하고, `flip_h`를 보고 좌우 보정의 부호도 맞춘다.
  - **죽는 순간 양쪽의 원화가 갈린다**(이슈 #176). `pose`가 어느 그림을 입을지 정하고, 바뀌면 `_apply_character()`를 다시 불러 배율과 여백 보정을 **처음부터 다시 잰다** — 눕는 패배 원화는 세로가 짧아 비율이 평소와 다르다(화면에서 79x72px 대 40~49x72px). 패배 포즈는 `_check_death()`가 건다 — 데미지가 들어가는 길이 전부 그 함수를 지난다. 라운드 대기 중에 남은 쪽이 또 떨어져도(그때 `main.gd`의 `_on_player_died()`는 일찍 돌아온다) 빠지지 않는 이유다. 승리 포즈만 `set_pose()`로 main.gd가 걸어 준다 — 누가 점수를 얻었는지 아는 곳이 거기다. `reset_round()`가 `POSE_IDLE`로 되돌린다(안 되돌리면 눕거나 팔을 든 채 다음 라운드를 싸운다).
  - 죽은 젤리를 **반투명하게 만들지 않는다** — 예전에는 `modulate.a = 0.35`로 죽음을 알렸는데, 흐리게 두면 눕는 원화의 땀방울·효과선이 어두운 맵(용암) 배경에 묻힌다. 이제는 포즈가 그 역할을 한다.
  - 젤리 찌그러짐은 속도·접지값에서 바로 나온다.
  - **관통(광선검 특수) 중에는 Player 자신의 `_draw()`가 몸 뒤에 민트빛 빛무리를 그린다**(이슈 #101). 자식(`Body`)보다 먼저 그려지므로 뒤에 깔려서 별도 노드가 필요 없고, 씬 루트에 건 가산 혼합은 **이 그리기에만** 적용되고 자식 스프라이트에는 영향이 없다. `_pierce_until` 하나만 보고 그린다. 관통은 "막기가 사라지는" 판정이라 표시가 없으면 켜졌는지 막힌 건지 사거리가 모자란 건지 구분할 수 없다 — 무기 색도 `PIERCE_TINT`(1을 넘겨 밝게)로 함께 바뀐다.
  - **평소와 다른 그림을 든 동안에는 `Smoke` 자식 노드가 무기 자리에서 작은 연기를 피운다**(`scripts/weapon_smoke.gd`). 그림이 바뀌는 계기는 둘이고 무기마다 하나만 쓴다 — `empowered_file`(`main.gd` 가 **미리 뽑아 둔** 강화: 강화 폭탄·빨간 표창)과 `ready_file`(특수 **쿨타임이 끝나** 쓸 수 있는 상태: 빨간 도끼). 둘 중 무엇인지 고르는 곳은 `_highlight_file()` 한 곳이고 **그림과 연기가 같은 판단을 쓴다**(조건을 두 곳에 쓰면 어긋난다). 연기 덩어리 수는 무기 표의 `smoke_puffs`(표창 5·양날 도끼 9), 색은 `Player.SPECIAL_SMOKE_COLOR`다. **루트가 아니라 자식에서 그리는 이유**는 `BlastRadius`와 같다 — 루트에 관통 빛무리용 가산 혼합이 걸려 있어서 거기서 그리면 연기가 아니라 빛이 된다. 연기는 배경을 덮어야 연기로 보인다. 덩어리 위상은 부모 노드 이름으로 씨앗을 잡아 **양쪽 화면에 같은 모양**이 뜨고(올라가는 시각은 각 컴퓨터의 시계라 어긋날 수 있다 — 판정과 무관하다), 켜고 끄는 것은 `empowered_ready`·`special_ready` 두 값만 보고 켠다. **`ready_file`을 쓰는 무기는 `_receive_special_ready()`에서 `_apply_weapon()`을 불러야 한다** — 매 프레임 도는 `_update_weapon_shape()`는 밝기·색만 손보고 텍스처는 갈지 않는다.
  - **무기 표에 `trail_ghosts`가 있는 무기는 `Trail` 자식 노드가 날이 지나간 자리에 미세한 잔상을 남긴다**(`scripts/weapon_trail.gd`, 이슈 #253 — 지금은 광선검뿐이다). 날 양끝은 자세를 다시 계산하지 않고 `_update_weapon_shape()`가 정해 둔 스프라이트의 `position`·`scale`·`rotation`·`offset`에서 되짚어 얻는다(`_blade_ends()`) — 세워 든 것·눕혀 든 것·휘두르는 중이 저절로 따라온다. 잔상은 **전역 좌표로** 들고 있다가 그릴 때 지역으로 되돌린다(지역으로 들고 있으면 젤리를 따라와서 지나간 자리에 남지 않는다). **연기와 같은 이유로 루트가 아니라 자식에서 그리고**, 여기서는 이유가 하나 더 있다 — 가산은 밝게만 만들 수 있어 평지 하늘 같은 밝은 배경에서 묻히므로(#112) 배경을 덮는 보통 알파로 그린다. `z_index`는 내리지 않고 **형제 순서**로 `WeaponSprite` 앞에 두어 날 밑에 깔린다(#146). 순간이동에서는 버린다(빨간 표창의 위치 교환·라운드 초기화) — 안 버리면 옛 자리와 새 자리를 잇는 줄이 한 번 그려진다. 따로 들고 있을 상태는 없다.
  - 무기는 그림이 있으면 `WeaponSprite`에 세워서 바라보는 쪽에 놓고(세로 `WEAPON_HEIGHT` 56px, 다만 가로가 `WEAPON_MAX_WIDTH` 80px를 넘으면 그쪽에 먼저 맞춘다 — 가로로 긴 원화(전기톱 2.69:1)가 몸통 3배 폭으로 터지는 것을 막는다, 이슈 #105), 쿨타임 상태는 밝기로 나타낸다. **뭉툭한 원화는 두 제한을 다 통과하고도 몸통만 해지므로**(세로 규칙이 가늘고 긴 무기 기준이다) 무기 표의 `weapon_art_scale`로 더 줄인다 — 지금은 글러브(1.18:1)·표창(1:1)·너클(1.06:1)이 0.6이다(이슈 #158·#173). 그림이 없는 2종은 여전히 `WeaponShape` 임시 막대이며 길이가 사거리·색이 쿨타임 상태다. 어느 쪽을 쓸지는 `_apply_weapon()`이 정한다. **그림으로 바꾸면 "길이가 사거리"라는 표시가 사라진다** — 장대가 그랬다. 사거리로 싸우는 무기는 무기 표의 `art_grows_with_reach`로 그림 쪽에 같은 표시를 만든다: 사거리 버프가 걸린 동안 그림의 **길이**가 그 배율(장대 1.6)로 늘고 굵기는 `ART_THICKEN_SHARE`(0.5)만큼만 붇는다(1.3배). 판정은 안 변한다(`_art_stretch()`). **장대는 무기 표의 `art_held_forward`로 유일하게 눕혀 든다** — 사거리 판정이 가로 방향이라 세워 들면 늘어나는 방향이 판정과 어긋난다. 눕힌 그림은 여백 보정을 `position`이 아니라 `Sprite2D.offset`으로 한다(offset은 회전·배율이 나중에 걸려 그림과 같이 돈다 — `projectile.gd`가 회전하는 탄에서 같은 이유로 쓴다). offset을 원화 길이의 절반만큼 더 올려 뒤쪽 끝을 원점으로 만들면 몸에 붙고 앞으로만 자란다. 세워 드는 무기가 늘어날 때는 대신 늘어난 길이의 절반만큼 위로 올린다(Sprite2D는 가운데를 기준으로 커진다). `sprite.scale`은 매 프레임 `_weapon_art_factor`에서 **다시** 계산한다(곱셈으로 누적하면 프레임마다 커진다).
- `scenes/light_burst.tscn` + `scripts/light_burst.gd` = **검 특수의 빛기둥 연출**(이슈 #99). 그림 파일 없이 `_draw()`로만 그려서 배경 없이 빛만 남고, 노드에 가산 혼합(`CanvasItemMaterial`, `blend_mode = 1`)이 걸려 있어 겹칠수록 하얘진다. 원점은 맞은 젤리의 발밑이고 0.9초 뒤 스스로 사라진다. 부챗살 모양·떨림은 `global_position`으로 씨앗을 잡은 난수라 **양쪽 화면에 같은 모양이 뜬다.**
- `scenes/swap_burst.tscn` + `scripts/swap_burst.gd` = **빨간 표창의 위치 교환 연출**. 빛기둥과 같은 규칙(그림 없이 `_draw()`, 가산 혼합, 위치로 씨앗을 잡아 늘 같은 모양, 0.45초 뒤 스스로 사라짐)이지만 **두 자리에 하나씩** 띄운다 — 하나만 띄우면 어디로 갔는지 알 수 없다. 원점은 발밑이 아니라 몸 한가운데(`main.gd`의 `SWAP_BURST_CENTER`)다. `main.gd` 가 투사체의 `swapped` 신호를 받아(`_on_positions_swapped`) `_play_swap_burst`를 부른다 — 투사체가 직접 띄우지 않는 것은 연출이 `Effects` 아래에 붙어야 하고 그 노드를 아는 것이 `main.gd`이기 때문이다.
- `scenes/lightning_strike.tscn` + `scripts/lightning_strike.gd` = **삼지창 특수가 맞았을 때 내려치는 자홍색 번개**. 빛기둥과 같은 규칙(그림 없이 `_draw()`, 가산 혼합, 위치로 씨앗을 잡아 늘 같은 모양, 0.4초 뒤 스스로 사라짐)이고 원점도 같이 **맞은 젤리의 발밑**이다 — 줄기는 화면 위(700px)에서 거기까지 내려온다. **줄기를 0.06초마다 갈아 끼워 떨게 한다**(`STRIKE_COUNT` 3) — 한 모양으로 가만히 있으면 번개가 아니라 분홍색 막대로 보인다. `main.gd` 가 투사체의 `struck` 신호를 받아(`_on_lightning_struck`) `_play_lightning_strike`를 부른다. 무기 표의 `hit_lightning`이 있는 무기만 스폰 데이터에 그 값을 실어 보낸다 — 지금은 삼지창뿐이다.
- `scenes/shockwave.tscn` + `scripts/shockwave.gd` = **양날 도끼가 착지할 때 바닥에 퍼지는 충격파**(이슈 #167). 빛기둥과 같은 규칙(그림 없이 `_draw()`, 가산 혼합, 이름으로 씨앗을 잡아 늘 같은 모양, 0.34초 뒤 스스로 사라짐)이고 원점은 **떨어진 자리(발밑)**다. 세로로 눌린 타원 고리가 밖으로 퍼지고 흙먼지가 튄다. **반경은 `main.gd`가 무기 표의 `landing_radius`를 그대로 넘긴다** — 보이는 크기와 실제로 맞는 범위가 어긋나면 이 연출이 거짓말이 된다. `main.gd` 가 `Player.landed_forced` 신호를 받아(`_on_forced_landed`) `_play_shockwave`를 부르고, **맞았는지와 무관하게** 띄운다(빗나간 것도 "여기까지였다"로 보여야 한다). 이 연출이 없으면 착지 범위 데미지는 화면에 아무 근거가 없다 — 폭탄 반경을 그린 이유(#140)와 같다.
- `scenes/shotgun_blast.tscn` + `scripts/shotgun_blast.gd` = **샷건 특수의 부채꼴 발사 연출**. 빛기둥과 같은 규칙(그림 없이 `_draw()`, 가산 혼합, 위치로 씨앗, 0.32초 뒤 스스로 사라짐)이고, 총구에서 갈래 22개가 부채꼴로 뻗는다. **사거리·각도를 무기 표에서 그대로 받는다** — 눈으로 배운 범위가 실제로 맞는 범위와 어긋나면 안 된다(폭탄 반경을 그린 이유 #140). 맞았는지와 무관하게 띄운다. **연출 노드는 위치·값을 `add_child` 전에 넣어야 한다** — `_ready()`가 붙는 순간 돌면서 위치로 난수 씨앗을 잡으므로, 나중에 넣으면 전부 (0,0)으로 같은 씨앗을 받는다(`light_burst.tscn`은 아직 이 순서다).
- `scripts/weapons.gd`(`class_name Weapons`) = **무기 표 17종**. 이름·기본/특수 데미지·쿨타임·넉백 등 모든 무기 수치의 유일한 출처. `RANDOM` 상수와 `resolve()`(랜덤 확정)도 여기 있다. 그림이 있는 15종은 `file` 필드를 갖고 `texture()`가 `assets/weapons/`에서 꺼내 온다 — 없으면 null이고 부르는 쪽이 막대로 대신한다. **선택창 그림이 손에 든 그림과 달라야 하는 무기는 `preview_file`을 따로 갖고 `preview_texture()`가 그쪽을 준다**(이슈 #173) — 지금은 너클만 쓰고, 파일이 없으면 조용히 `file`로 되돌아간다. **사거리 배율을 상시로 주지 말 것** — 막기 판정(`main.gd`의 `is_blocked()`)이 "상대 사거리 > 내 사거리"라서, `reach_multiplier`가 1.0을 넘는 무기는 특수를 쓰지 않아도 상대 근접 공격을 전부 막는다. 장대가 1.6으로 그랬고 마주 서면 상대가 데미지를 하나도 못 넣었다. 조금 줄이는 것으로는 안 된다(부등호 하나라 1.0 초과면 전부 막는다) — 사거리 우위는 `special_reach_multiplier`로 특수 동안만 준다.
- `scripts/maps.gd`(`class_name Maps`) = **맵 표 5종**(바다·화산·협곡·오두막·투기장). 이름·씬 경로·**맵 미리보기 원화**(`art`, 이슈 #289)의 유일한 출처. `art_texture()`가 `assets/maps/`에서 꺼내 오고 없으면 null이라 부르는 쪽이 `art_color()`의 단색으로 대신한다(무기 그림이 없을 때와 같은 방식) — 바다만 원화가 없어 `color`를 적어 두었고, 목록에 없는 이름(`랜덤`·빈 자리)은 `UNKNOWN_COLOR`가 받는다. `RANDOM` 상수와 `resolve()`(랜덤 확정)가 무기 표와 같은 형태다. **맵 씬 계약**: 루트 `Node2D`, `Spawns/Spawn1`·`Spawn2`(Marker2D, 순서가 1P·2P), 지형은 `StaticBody2D` + `CollisionShape2D`, 즉사 구역은 `Hazard`(Area2D), 배경도 맵이 그린다. 좌우 벽이 없는 맵은 화면 밖으로 나가면 낙사한다.
  - 바다·화산에는 `Hazard`가 있어 닿으면 즉사한다. 오두막·투기장은 좌우 벽이 있고 `Hazard`도 없어 낙사가 일어나지 않는다. 협곡은 바닥이 없어 가운데로 떨어지면 화면 밖 낙사다.
  - **바다를 뺀 4종(화산·협곡·오두막·투기장)은 지형을 `ColorRect`로 그리지 않고 `assets/maps/`의 배경 원화 한 장을 `Background`(Sprite2D)로 깐다**(이슈 #264). 원화는 1920x1080이고 게임 좌표는 1152x648이라 `position = (576, 324)` + `scale = 0.6`이면 정확히 들어맞고, **원화 픽셀 x 0.6이 곧 충돌 상자 좌표**다. 충돌은 그림에 그려진 발판만 따라가고 집·간판·기둥 위 장식은 배경으로 남긴다.
  - **발판 사이 높이차는 160px을 넘기지 않는다** — 점프 높이가 `560²/(2·980)` = 160px이라, 넘기면 올라갈 수 없는 발판이나 한 번 빠지면 못 나오는 구덩이가 생긴다. 새 맵을 그릴 때 가장 먼저 확인할 값이다.
  - **높은 발판은 화면 위 `y ≲ 182`를 피한다** — 전투 화면 HUD 카드가 `y 8~110`(x 8~372·780~1144)을 덮고 있어서, 그보다 높은 곳에 서면 젤리 72px가 카드에 가려진다. **협곡의 절벽 꼭대기(119·128)가 아직 걸려 있다** — 화면 맨 위라서 카드를 더 올릴 자리가 없다(#267). 화산의 위쪽 발판(188·193)은 카드를 올리면서 풀렸다.
- `scripts/art.gd`(`class_name Art`) = 그림 공통 처리. `content_rect()`가 **투명 여백을 뺀 실제 그림 영역**을 잰다. 캐릭터·무기 원화가 모두 정사각 캔버스에 여백을 두고 그려져 있어 크기와 위치를 잡을 때 항상 이 값을 기준으로 한다. `draw_glow()`는 **가운데가 밝은 빛무리**를 그린다 — 원 하나로 그리면 테두리가 딱 끊겨 어두운 판때기로 보여서, 같은 옅기의 원을 크기만 줄여 가며 겹쳐 쌓는다. 빛기둥과 관통 표시가 같이 쓴다.
- `scripts/combat.gd`(`class_name Combat`) = 전투 공통 수치. MAX_HP 100, INVULNERABLE_TIME 0.1, MELEE_HIT_INTERVAL 0.6(**지속 데미지 무기의 데미지 간격에는 안 걸리고 넉백 간격으로만 쓰인다** — 이슈 #103), ROUND_START_GRACE 2.0, POINTS_TO_WIN 3, ROUND_RESTART_DELAY 2.0, MATCH_END_DELAY 4.0, 넉백 3단계(200/400/700), PROJECTILE_SPEED 1120, 낙사 경계 `is_out_of_bounds()`.
- `scenes/projectile.tscn` + `scripts/projectile.gd`(Area2D, `class_name Projectile`) = 허공을 나는 것(화살·총알·표창·던진 단검·폭탄). 이동도 판정도 스스로 하고, 만들고 없애는 것은 `main.gd`다. 상대 무기에 막히지 않고 공유 무적도 타지 않는다.
  - **폭발 반경은 `BlastRadius` 자식 노드가 그린다**(`scripts/blast_radius.gd`, 이슈 #140) — `explosion_radius`(폭탄 200px)만큼의 반투명 원과 그 경계선이다. 폭탄은 닿지 않아도 맞는 유일한 무기라 범위가 안 보이면 피할 거리인지 판단할 근거가 없다. **루트가 아니라 자식에서 그리는 이유**는 루트에 미사일 불꽃용 가산 혼합이 걸려 있어서다 — 가산은 밝게만 만들 수 있어 평지 하늘처럼 흰 배경 위에서는 아무것도 안 보인다(#112). 자식은 그 재질을 물려받지 않아 보통의 알파 혼합으로 그려진다. 폭탄 그림 뒤에 깔리는 것은 **형제 순서**로 한다(`Visual`·`ArtSprite`보다 앞에 있다) — **`z_index`를 내리면 안 된다**(이슈 #146). z를 내리면 z 0에 있는 것 **전부보다** 먼저 그려지는데 맵이 자기 배경을 z 0의 불투명 `ColorRect`로 깔기 때문에 그 아래로 사라진다. `Effects`처럼 **올리는** 것은 안전하고 내리는 것만 위험하다. 원점 기준으로 한 번만 그려 두면 폭탄이 움직여도 따라간다. 반경이 0인 나머지 투사체는 아무것도 그리지 않는다.
  - 그리기는 기본이 노란 막대(`Visual`)지만, 스폰 데이터에 `art`(무기 이름)가 있으면 `ArtSprite`에 그 무기 그림을 붙이고 진행 방향으로 회전시킨다 — 단검·삼지창·로켓 글러브가 쓴다(이슈 #96·#152·#161). **원화의 앞이 오른쪽이면 `art_points_right`를 넘긴다** — 기본은 "날 끝이 위"라 90도를 더해 돌리는데, 가로로 그린 로켓 글러브는 그러면 주먹이 위를 본다. `max_distance`를 주면 그만큼 날아간 뒤 사라진다(글러브 300px — 기획서의 "단거리"를 지키는 선이다). `speed`를 주면 그 속도로 나간다(글러브 700px/s, 없으면 공통 1120px/s). 방향은 **위치 변화**로 잡는다(`_process`) — 유도로 휘어도 그림이 따라 돈다. 멈추면 마지막 방향을 유지해 바닥에 꽂힌 모양이 된다. 회전하는 그림이라 여백 보정은 `position`이 아니라 `Sprite2D.offset`으로 넣어야 한다.
- `docs/weapon-system.md` = 무기 추가·수정 방법과 지켜야 할 계약. `docs/무기_수치_초안.md` = 수치가 정해진 근거와 미확정 항목.
- `assets/fonts/BlackHanSans-Regular.ttf` = **화면 글꼴** (요청). 초굵기·좁은 폭의 한글 디스플레이 폰트(Google Fonts, SIL OFL 1.1 — 라이선스 전문은 같은 폴더의 `BlackHanSans-OFL.txt`).
- `resources/display_font.tres` = 그 폰트를 감싼 `FontVariation`이고 **테마가 이것을 가리킨다**. `fallbacks`에 `korean_font.tres`를 물려 둔 것이 핵심이다 — Black Han Sans에는 이모지가 없어서 폴백이 없으면 `랜덤 🎲` 의 주사위가 네모로 나온다. 글꼴을 또 바꿀 일이 생기면 `base_font`만 갈고 폴백은 그대로 둔다.
- `resources/korean_font.tres` = 한글 계통 폰트 리소스 (SystemFont). 이제 **직접 쓰이지 않고 폴백으로만** 쓰인다.
  - `_draw()`로 글자를 그리는 곳(`point_gain.gd`)은 테마를 타지 않으므로 `display_font.tres`를 직접 preload한다. 새로 그런 곳을 만들면 같은 것을 가리킬 것 — `korean_font.tres`를 들면 그 연출만 옛 글꼴로 남는다.
- `resources/ui_theme.tres` = **UI 공통 테마**. `project.godot`의 `gui/theme/custom`으로 프로젝트 전체에 걸려 있어 버튼·라벨·패널·입력칸·진행바의 기본 모양이 여기서 나온다. **색을 바꾸려면 여기를 고친다** — 씬마다 `theme_override`를 넣지 말 것. 젤리 톤 팔레트: 크림 배경 `(0.99, 0.95, 0.92)`, 진한 글자 `(0.29, 0.23, 0.32)`, 보조 글자 `(0.44, 0.39, 0.48)`, 젤리 핑크 `(0.96, 0.55, 0.78)`, 라벤더 `(0.56, 0.59, 0.91)`. 흰 카드 + 큰 둥근 모서리(버튼 18·패널 28) + 부드러운 그림자가 기본형이다.
  - **파스텔 핑크·라벤더는 장식용이고, 흰 글자를 얹는 면에는 진한 쪽을 쓴다**(이슈 #112). 파스텔 위의 흰 글자는 대비가 2.2~2.7:1밖에 안 나와 글자가 배경에 뜬다. 주 동작 버튼·선택된 방 버튼·체력 막대 채움은 **진한 핑크 `(0.8, 0.29, 0.56)`**(hover `(0.86, 0.36, 0.63)`·pressed `(0.68, 0.22, 0.46)`·테두리 `(0.62, 0.17, 0.4)`)와 **진한 라벤더 `(0.42, 0.45, 0.82)`**(hover `(0.48, 0.51, 0.86)`·pressed `(0.34, 0.37, 0.74)`·테두리 `(0.3, 0.33, 0.68)`)를 쓴다 — 이 조합은 4.2:1이다. 파스텔 두 색은 젤리 미리보기·결과 글자처럼 **외곽선이 대비를 대신 받쳐 주는 곳에만** 남겼다.
  - 글자는 **18px이 바닥**이다. 크림·흰 배경 위 본문은 4.5:1, 색 버튼의 큰 글자는 3:1을 넘긴다. 보조 글자를 옅게 하고 싶어도 `(0.44, 0.39, 0.48)`보다 밝히지 않는다.
  - 체력 막대는 트랙 `(0.9, 0.86, 0.9)`에 2px 테두리 `(0.76, 0.7, 0.78)`가 있다. 흰 카드 위에 트랙만 얹으면 막대가 **어디서 끝나는지** 안 보여서 남은 비율을 읽을 수 없다.
  - 예외적으로 씬에 남긴 `theme_override`는 **화면마다 하나뿐인 주 동작 버튼**(타이틀 `StartButton`·선택 창 `GoButton`은 핑크, `RandomButton`은 라벤더)과 글자 크기·색 같은 개별 값이다. 새 버튼은 기본 흰 카드 모양을 그대로 쓰는 것이 원칙이다.


**한 화면에 둘이 앉으므로 액션이 자리마다 한 벌씩, 모두 10개다**(이슈 #320): `p1_left`·`p1_right`·`p1_jump`·`p1_fast_fall`·`p1_skill`, `p2_…`. 1P는 `A`/`D`·`W`·`S`·왼쪽 `Shift`, 2P는 `←`/`→`·`↑`·`↓`·`Space`다 — 온라인 전환(#33) 이전에 쓰던 배치를 그대로 되살렸다. 읽는 곳은 `Player.read_input()` 하나이고 이름은 `GameState.action(player_id, name)`이 만든다. 전투 중 ESC(`ui_cancel`)로 title 복귀.
**기본 공격에는 입력이 없다** — 근접은 닿으면, 원거리는 간격마다 `main.gd`가 자동으로 판정한다. `skill`은 특수 공격 전용이고, 무기 선택 카드에서는 좌우 이동 키와 함께 카드를 고르는 데도 쓴다.

### 미구현 (로드맵 #32 기준)

기획서의 냉장고·봉지 속·위 속 맵 — 이슈 #62에서 바다·용암·벽돌로 교체했다. 그때 들어온 용암·벽돌과 첫 맵이던 평지는 지형을 `ColorRect`로 그린 초기 맵이라 **이슈 #286에서 다시 제거했다**(씬 파일까지 삭제). 되살리려면 #286 이전 커밋에서 씬을 꺼내 `Maps.LIST`에 다시 적는다.
맵 고유 기믹(움직이는 발판 등)과 배경 애니메이션(물결·용암 흐름)은 없다.
무기별로 남은 것(삼지창 회수 연출, 미확정 수치)은 `docs/weapon-system.md`의 "아직 안 된 것"에 정리되어 있다.
온라인 대전·관전은 이슈 #320에서 걷어냈다 — 두 사람이 한 기기 앞에 앉아야 한다.

무기·전투(4단계)는 #46에서 공동작업자(@Kadsa-MXZI)의 `feat/online-multiplayer-and-weapons` 브랜치에서 이식했다. 그 브랜치에는 독자적인 네트워크·선택 창 구현(`net.gd`, 자체 `lobby.gd`, `server/` 도커)도 들어 있지만 **가져오지 않았다.**

### 개발 시 주의

- **Godot 바이너리가 있다** — `~/Downloads/Godot_v4.6.2-stable_win64.exe/Godot_v4.6.2-stable_win64_console.exe`. **커밋 전에 반드시 돌린다:**

  ```
  timeout 20s "<godot>" --headless --path . 2>&1 | grep -E "SCRIPT ERROR|\.gd:|Parse Error|Compile Error"
  ```

  헤드리스로도 게임이 그대로 도므로(#320 이후로는 헤드리스가 곧 서버였던 규칙이 없다) `timeout`으로 끊는다.
  끊을 때 나오는 `BUG: Unreferenced static string ...`은 엔진 종료 잡음이니 무시하고, 위 필터에 걸리는 것만 본다.
  경로·상수 대조 같은 정적 확인만으로는 **타입 추론 실패·타입 불일치가 잡히지 않는다**(이슈 #66·#69에서 두 번 놓쳤다).
  `--check-only --script`는 오토로드를 안 올려서 `GameState` 같은 식별자를 못 찾는다고 헛짚으니 쓰지 말 것 —
  대신 임시 오토로드 하나를 붙여 `res://scripts`·`res://scenes`·`res://resources`의 파일을 전부
  `ResourceLoader.load()` 해 보면 컴파일 오류가 한 번에 잡힌다(#320에서 `files=66 failures=0` 으로 확인했다).
  사람 손 없이 한 경기를 돌려 보려면 같은 방식의 오토로드가 `title._on_start_pressed()` →
  `select._on_go_pressed()` → `weapon_pick._on_card_pressed(0)` → `player.kill()` 을 눌러 주면 된다.
- 그래도 **화면으로 봐야 아는 것**(색·배치·발판 높이·조작감)은 사용자 F5 확인이 필요하다. 헤드리스 실행은 오류 유무만 알려준다.
- `_ready()`에서 `change_scene_to_file()`을 바로 부르면 "Parent node is busy" 오류가 난다 — `call_deferred`로 미룬다.
- **`@rpc`·`multiplayer`·`Network`·`Lobby` 를 다시 들이지 않는다**(이슈 #320). 오프라인 한 화면 2인이 지금의 구조다 — 판정과 그림이 한 프로세스에 있고, 복제할 곳이 없다. 온라인이 다시 필요해지면 그때 이력에서 꺼내 되살릴 일이지, 남겨 둔 껍데기에 얹을 일이 아니다.
- **`export_presets.cfg`에 프리셋이 하나다**(이슈 #320에서 관전 빌드를 지웠다) — 산출물이 아니라 빌드 방법이므로 `.gitignore`에서 빼서 커밋한다. 다만 **이 개발 환경에는 export 템플릿이 없어 실제 `.exe`를 만들 수 없다**(`~/AppData/Roaming/Godot/export_templates/`가 비어 있다). 프리셋이 파싱되는지는 `--export-release`가 "템플릿 없음"까지 가는 것으로 확인하고, 빌드는 사용자가 에디터에서 한다. 방법은 `docs/build.md`.
- `README.md`는 프로젝트와 무관한 외부 유저가 보는 문서다 — 폴더 구조, 확장 가이드, 엔진 실행·검증 방법을 넣지 않는다(이슈 #4·#8·#11). 개발자용 정보는 이 파일과 `docs/`에 둔다. 현재 README에는 리셋과 함께 폴더 구조·실행 방법이 다시 들어가 있어 정리가 필요하다.
- **표(`Weapons.LIST`·`Characters.LIST`·`Maps.LIST`)에서 꺼낸 값은 `Variant`다.** `var path := DIR + dict.get("file", "")`처럼 `:=`로 받으면 타입 추론이 실패해 **스크립트가 파싱되지 않고 게임이 아예 뜨지 않는다**(이슈 #66). 반드시 `var file: String = ...`처럼 명시 타입으로 받는다. 정적 검증이 잡아내지 못하는 종류라 표를 다루는 코드를 쓸 때마다 확인한다.
- .gd 스크립트를 새로 만들면 사용자 에디터가 .uid 파일을 생성한다 — 발견 시 해당 이슈 브랜치에 커밋한다.
- .tscn의 `load_steps`는 Godot 4.6이 더 이상 기록하지 않는다 — 이미 있는 파일에서는 값을 유지하고(= ext_resource 수 + sub_resource 수 + 1), 없는 파일에 손으로 추가하지 않는다.
- **사용자가 에디터에서 씬을 만지면 파일이 정규화된다** — `load_steps`가 빠지고, 노드에 `layout_mode`·`anchors_preset`·`unique_id`가 붙고, 손으로 적어 둔 자리표시 UID(`uid://jellyselect01` 같은 것)가 진짜 UID로 교체된다. 실제 변경은 몇 줄인데 diff가 크게 보이니 놀라지 말 것. 이때 **참조 쪽만 새 UID로 바뀌고 선언 쪽은 옛 UID로 남아 어긋날 수 있다**(#72에서 `player_panel.tscn`이 그랬다) — 씬을 커밋하기 전에 선언과 참조를 대조한다. 아직 자리표시 UID인 씬(`main`·`player`·`title`·`projectile`)도 열어 저장하는 순간 같은 일이 일어난다.
- **새 씬을 손으로 만들 때는 `ext_resource`에 `uid=`를 적지 말고 `path=`만 적는다**(이슈 #189). 자리표시 UID를 적어 두면 엔진이 한 번 도는 순간 **선언 쪽만** 진짜 UID로 교체되어 참조와 어긋난다(`room_switcher.tscn`이 그랬다). 그때 참조를 그 진짜 UID로 맞춰 줘도 `.godot` 캐시가 아직 모르면 `invalid UID ... using text path instead` 경고가 뜬다 — `path`만 두면 경고 없이 해결되고, 사용자가 에디터에서 저장할 때 Godot이 알맞은 UID를 스스로 붙여 준다. 저장소의 다른 손수 만든 참조들(`main.tscn`의 스크립트 참조 등)도 `path`만 쓴다.
- .tscn의 `uid://`는 손으로 바꾸지 않는다. 씬의 자기 UID를 바꾸면 그 씬을 참조하는 `ext_resource`의 UID도 같이 고쳐야 한다. stale `.godot` 캐시 상태로 에디터가 UID를 재생성하면 참조가 끊길 수 있으니(이슈 #27), 그런 변경은 커밋하지 말고 `git restore`로 되돌린다.
- `main`이 리셋된 이력이 있다. `dev`는 2026-07-26에 `main`(`e2a7dcb`) 지점으로 재정렬했다. 옛 히스토리가 필요하면 `backup/main-before-reset`을 참조한다.
- **`dev`는 2026-08-20에 원격에서 삭제되었다가 2026-08-22에 `main`(`636df2f`) 지점으로 다시 만들어졌다**(이슈 #186). PR #174가 dev로 머지된 직후 브랜치 정리 중에 지워진 것으로, 그 사이에 진행한 #180·#182·#184는 `dev`가 없어 **`main`으로 직접 머지했다** — 그 세 건에 dev PR이 없는 것은 흐름을 어긴 것이 아니다. 삭제 시점의 `dev`가 들고 있던 고유 커밋 10개는 전부 "PR → dev" 머지 커밋이고 그 내용은 이미 `main`에 별도 PR로 들어가 있었으므로(양방향 diff가 대칭이었다) **파일 내용 손실은 없다.** 로컬에 옛 `dev`가 남아 있으면 `git fetch && git reset --hard origin/dev`로 맞춘다.

## Issue-First Rule

- 기능, 버그 수정, 개선, 리팩터링 작업은 GitHub 이슈 없이 구현을 시작하지 않습니다.
- 사용자가 이슈 없이 작업을 요청하면 GitHub 접근 권한이 있을 때 먼저 이슈를 만듭니다.
- GitHub 접근 권한이 없으면 사용자에게 이슈 없이 진행해도 되는지 확인하고, 최종 응답에 이슈 생성이 막혔다는 점을 남깁니다.
- 이슈 번호는 브랜치 이름, 커밋 메시지, PR 본문에 포함합니다.
- 관련 없는 정리 작업은 별도 이슈와 별도 브랜치로 분리합니다.

## Required Issue Detail

모든 기능, 개선, 버그 이슈에는 아래 항목이 있어야 합니다.

- Summary: 무엇이 바뀌어야 하는지.
- Motivation or Problem: 왜 필요한지.
- Current Behavior: 현재 어떻게 동작하는지.
- Expected Behavior: 완료 후 어떻게 동작해야 하는지.
- Scope: 영향을 받을 게임 시스템, 씬, 스크립트, 에셋, 문서.
- Acceptance Criteria: 완료를 증명할 구체적인 기준.
- Verification Plan: 실행할 명령이나 수동 확인 방법.

버그 수정 이슈에는 추가로 아래 항목이 필요합니다.

- Reproduction Steps.
- Actual Result.
- Expected Result.
- Environment, when relevant.

새 기능 이슈에는 추가로 아래 항목이 필요합니다.

- Player Flow.
- Non-goals.
- UX, input, balance, or settings expectations, when relevant.

## Branching

- 이슈 하나당 브랜치 하나를 만듭니다.
- 브랜치 이름은 짧고 이슈 번호를 포함합니다.
- 권장 형식:
  - `issue-<number>-short-topic`
  - `fix-<number>-short-topic`
  - `feat-<number>-short-topic`

## Implementation

- 파일을 수정하기 전에 이슈를 읽고 의도한 동작을 확인합니다.
- 변경 범위는 이슈에 적힌 내용으로 제한합니다.
- 기존 프로젝트 패턴을 우선합니다.
- 큰 구조 변경이나 폴더 정리는 해당 이슈가 직접 요구할 때만 합니다.

## Verification

변경한 파일과 게임 엔진 상태에 맞춰 가장 작은 의미 있는 검증부터 실행합니다.

- Godot 프로젝트 설정 확인
- 변경한 씬 또는 스크립트 수동 실행
- 플레이어 입력, UI, 충돌, 게임 흐름 확인
- 사용 가능한 테스트나 빌드 명령이 생기면 해당 명령 실행

PR 본문에는 실제로 확인한 내용을 기록합니다.

## Pull Requests

- PR 제목은 이슈에서 해결한 결과를 요약합니다.
- PR 본문에는 `Closes #<issue-number>`를 포함합니다.
- PR 본문에는 summary, verification, residual risks를 포함합니다.
- 검증 내용이 기록되기 전에는 머지하지 않습니다.

## Merge Flow

- 이슈를 해결하고 검증을 마친 뒤 이슈 브랜치에 커밋하고 원격 저장소에 푸시합니다.
- 이슈 브랜치에서 `dev`로 첫 번째 PR을 엽니다.
- `dev` 대상 PR이 mergeable/CLEAN이고 변경 파일이 이슈 범위와 일치하면 자동으로 머지합니다.
- 같은 원격 이슈 브랜치에서 `main`으로 두 번째 PR을 엽니다.
- **`main` 대상 PR도 같은 조건이면 자동으로 머지합니다**(이슈 #115). mergeable/CLEAN이고 변경 파일이 이슈 범위와 일치하고 아래 중단 조건에 걸리지 않으면 `dev` 머지 후 이어서 머지합니다. 사람 리뷰를 기다리며 멈추지 않습니다 — 팀원 대부분이 코드를 읽지 못해 리뷰가 검증 역할을 하지 못하고, 리뷰 요청만 쌓인 채 `main` 반영이 막혔습니다.
- 브랜치 보호 설정을 admin 권한으로 우회하지 않습니다(`gh pr merge --admin` 금지). 지금은 `main`·`dev` 모두 보호가 걸려 있지 않지만, 나중에 켜면 이 금지가 그대로 적용됩니다.
- `main` 머지가 끝난 뒤 원격 이슈 브랜치를 삭제합니다.
- 정리 후 로컬 저장소는 삭제된 이슈 브랜치가 아니라 `main` 또는 `dev`에 둡니다.
- 팀 협업 규칙과 브랜치 보호 설정은 `docs/collaboration.md`를 따릅니다.

자동 머지를 멈추고 사용자에게 보고하는 예외. **`dev`와 `main` 양쪽에 적용됩니다:**

- 코드 충돌이 있거나 mergeable/CLEAN이 아닌 경우
- 이슈 범위 밖의 파일 변경이 섞인 경우
- 검증이 누락되었거나 미완인 경우(예: 자격증명·바이너리 부족으로 실행 확인 불가)
- 사용자가 "머지하지 말라"고 지시한 경우
- 되돌리기 어려운 부수효과가 있는 경우(데이터 마이그레이션, 배포 트리거 등)

`main`에만 걸리던 예외("화면으로만 판단되는 변경은 `dev`까지만 머지하고 멈춘다", 이슈 #115)는 **없앴습니다**(이슈 #142). UI 색·배치·글자 크기, 맵 지형 높이, 조작감처럼 눈으로 봐야 아는 변경도 위 조건만 맞으면 `main`까지 이어서 머지합니다. **공동작업자 리뷰를 기다리지 않는 것과 같은 이유입니다** — 화면 확인은 사용자가 하는 것이고, 그것 때문에 머지를 붙들고 있을 이유가 없습니다.

- 그 예외에 걸리는 것이 UI·연출·맵 작업 **대부분**이라, 자동 머지를 열어 둔 것이 실질적으로는 열리지 않은 것과 같았습니다. 확인을 기다리는 동안 `main` 반영이 계속 밀렸습니다.
- **F5로 무엇을 봐 달라는 보고는 그대로 합니다.** 머지를 멈추지 않을 뿐 확인 요청을 없애는 것이 아닙니다. 헤드리스 실행은 여전히 **오류 유무만** 알려주므로, PR 본문과 사용자 보고에 무엇을 눈으로 봐야 하는지 구체적으로 적습니다.
- 화면이 잘못 나온 것을 나중에 알게 되면 되돌리지 말고 **새 이슈로 고칩니다.** 그리기만 바뀐 변경은 고치는 쪽이 되돌리는 쪽보다 싸고 이력도 깔끔합니다.
